<div class="container pb-5">

	<p>Jeśli chcesz zostawić komentarz zaloguj się poprzez rejestrację w zakładce zaloguj się/zarejestruj się</p>

	<%= simple_form_for @opinion do |f| %>

	    <%= f.input :name, label: 'Podaj tytuł:' %>
	    <%= f.input :description, label: 'Podaj treść:', as: :trix_editor%> 
	    <%= link_to 'Powrót', opinions_path, class: 'btn btn-info btn-lg' %><hr>
	   <div class="pb-5"> 
	   		<%= f.submit "Zapisz", class: 'btn btn-success', style: "float:left" %>
	   </div>

	<% end %>
</div>