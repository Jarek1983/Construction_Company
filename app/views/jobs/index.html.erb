<section class="container pt-5 pb-5">
  <div class="row">

    <div class="p-2">

      <%= will_paginate @jobs, renderer: WillPaginate::ActionView::BootstrapLinkRenderer %>

    </div>
    
      <div class="col-12">

            <% if current_user && current_user&.admin? %>

            <%= link_to 'Dodaj', new_job_path, class: "btn btn-primary btn-lg" %> <hr/>

            <% end %>

          <div class="row pb-5">
              <% @jobs.each do |job| %>
                <div class="col-12">
                  <div class="card"> 
                          <div class="card-block">
                               <h4 class="card-title text-center font-weight-bold pt-3">
                                  <%=job.name %>
                               </h4>
                               <p class="card-title text-justify mx-2">

                                <p class="lead ml-3 font-italic">Obowiązki: <%=sanitize job.description %>
                               </p>
                               <p class="lead ml-3 font-italic">Wymagania: <%=sanitize job.specifications %>
                               </p>
                               <p class="lead ml-3 font-italic">
                                Oferujemy: <%=sanitize job.offer %>
                               </p>
                               <p class="lead ml-3 font-italic">
                                Dodatkowe informacje <%=sanitize job.information %>
                               </p>                               

                                <% if current_user && current_user&.admin? %>

                                  <div class="p-2">
                                    <%= link_to 'Pokaż', job_path(job), class: "btn btn-lg btn-primary" %>
                                    <%= link_to 'Edytuj', edit_job_path(job), class: "btn btn-lg btn-info" %>
                                    <%= link_to 'Usuń', job_path(job), method: :delete, data: {confirm: 'Czy na pewno usunąć?'}, class: "btn btn-lg btn-danger" %>
                                  </div>
                                <% end %>

                          </div>
                  </div>
                </div>
              <% end %>
          </div>

      </div>
  </div>
</section>
