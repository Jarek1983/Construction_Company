<div class="container pb-5">

	<%= simple_form_for @execution do |f| %>

	    <%= f.input :name, label: 'Nazwa' %>
	    <%= f.input :description, label: 'Opis prac', as: :trix_editor%> 
	    <%= f.hidden_field :user_id, value: 1 %>
	    <%= link_to 'Powrót', executions_path, class: 'btn btn-info btn-lg' %><hr>
	   <div class="pb-5"> 
	   		<%= f.submit "Zapisz", class: 'btn btn-success', style: "float:left" %>
	   </div>

	<% end %>
</div>